import "./loading.css";

const loading = () => {
  return (
    <section className="container-loading">
      <div className="loading-bar">Loading</div>
    </section>
  );
};

export default loading;
